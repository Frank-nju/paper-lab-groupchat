# CUMCM attack checklist

- 0:00 决策是否偷看当天后续真实量；预测值与实际值是否混用；
- 滚动优化是否只使用当时可获得的信息；
- 储能/库存/队列等状态转移是否守恒、效率是否重复计算；
- 极端边界：容量为 0、负荷峰值、光伏为 0、价格突变、不可行约束；
- 优化结果是否给可行性检查、替代求解/小例 oracle；
- 预测是否严格按时间切分，禁止未来样本进入训练；
- 指标是否与题目目标一致；
- 敏感性、稳健性和 baseline 是否足以支持“优于”类结论；
- 每个小问是否有独立的输入—模型—输出—验证闭环。
