# 本次迁移说明

来源是工作区对话“搭建论文群聊优化流程”中确认的设计：AG2 0.9.10、GPT-6 建模审查、Fable 5.1 数学审查、低价模型执行、终端实时显示和 JSONL/Markdown 记录。

本地版本做了以下适配：

- 新建独立目录 `paper-lab-groupchat`，不改动工作区原有论文、代码和压缩包。
- Windows 通过 `run_windows.ps1`/`run_windows.bat` 启动，Linux/macOS 通过 `run_linux.sh` 启动。
- 配置、论文、日志和输出都用 `pathlib` 相对项目根目录解析，避免云端 Linux 路径泄漏到 Windows。
- GPT-6 与 Fable 5.1 采用同级 `AssistantAgent`，用固定选择器保证每人一句后才轮到廉价模型。
- 输入论文保持只读，低价模型的工作产物写到 `outputs/`，方便人工检查后再应用。
- 没有 API Key 时可离线运行，便于先验证环境、路径和记录流程。
